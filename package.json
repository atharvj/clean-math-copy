{
  "name": "clean-math-copy-userscript",
  "version": "1.2.2",
  "private": true,
  "description": "A userscript that copies rendered math and ordinary selections as clean, dependable plain text.",
  "main": "clean-math-copy.user.js",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "syntax": "node --check clean-math-copy.user.js",
    "check": "npm run syntax && npm test",
    "browser-smoke": "node tests/run-browser-smoke.js"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "katex": "^0.17.0"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "license": "MIT"
}
